import 'package:flutter/material.dart';

class GroupsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
          'List of groups, like journal clubs or associations, where you can chat or message board with.'),
    );
  }
}
