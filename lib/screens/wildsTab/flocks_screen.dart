import 'package:flutter/material.dart';

class FlocksScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
          'Like a group, but renamed to feel light and fun, like birds in the air, coming together and apart at will. List of groups, like journal clubs or associations, where you can chat or message board with.'),
    );
  }
}
