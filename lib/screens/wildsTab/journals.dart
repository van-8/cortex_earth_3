import 'package:flutter/material.dart';

class JournalsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('List of subscribed Journals, favs on top'),
    );
  }
}
