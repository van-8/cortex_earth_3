import 'package:flutter/material.dart';

class ExperimentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
            'Design and conduct an experiment directly within the app, loaded onto phone or iPad or computer. Connect to sensors, etc.'),
      ),
    );
  }
}
