import 'package:flutter/material.dart';

class SketchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Sketch down a diagram before you forget it.'),
      ),
    );
  }
}
