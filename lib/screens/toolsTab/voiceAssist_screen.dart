import 'package:flutter/material.dart';

class VoiceAssistScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Voice assist tools to help, maybe sort tags?'),
    );
  }
}
