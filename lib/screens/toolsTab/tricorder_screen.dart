import 'package:flutter/material.dart';

class TricorderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('linkup with Tricorder, external tools and sensors.'),
    );
  }
}
