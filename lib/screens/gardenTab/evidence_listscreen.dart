import 'package:flutter/material.dart';

class EvidenceListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('List of saved evidence.'),
    );
  }
}
