import 'package:flutter/material.dart';

class SynapsesListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
          'List of saved snippets from other articles & books, ready for citation to your own projects.'),
    );
  }
}
