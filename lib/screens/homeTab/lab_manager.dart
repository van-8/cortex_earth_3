import 'package:flutter/material.dart';

class LabManager extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Manage your labs Code of conduct, etc'),
    );
  }
}
